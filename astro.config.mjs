import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  projectRoot: "./",
  publicPath: "/staging.fishermen-analytics.com/goodday/",
  dist: "./dist",
});
